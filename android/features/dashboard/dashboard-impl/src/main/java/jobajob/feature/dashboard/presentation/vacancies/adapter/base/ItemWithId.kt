package jobajob.feature.dashboard.presentation.vacancies.adapter.base

/**
 * A base class for simple RecyclerView items (like footers, headers, progress indicators, etc.)
 */
internal abstract class ItemWithId {
    abstract val id: String
}