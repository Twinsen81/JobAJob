apply from:"$rootDir/${GradleTemplates.androidFeature}"

dependencies {
    implementation project(Modules.libraryUiComponents)
    implementation project(Modules.featureFavoritesApi)

    // Retrofit
    implementation Libraries.okhttp
}